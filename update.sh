#!/usr/bin/env bash
sleep 1m && git clone https://${SECRET_TOKEN}@github.com/rushimoto001/testdeploy -b weeb gitpush && cd gitpush && git config --global user.email "rushimoto001@gmail.com" && git config --global user.name "rushimoto001" && git config --global color.ui true && perl -i -pe '/^ver=/ && s/(\d+)/$&+1/e' count.txt && git add . && git commit --allow-empty -m "Upgrade Count on $(date -u +"%D %T%p")" && git push -q
